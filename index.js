/*
Variables defined in the test suite:
- oldID
- currentAge

You should NOT try to redefine any of the above variables.
*/

// Write your code below this line!


const newID = (1000000000 + oldID); 

// we assume we have a variable called currentAge. the test will set this var
 // write some code here that is true if currentAge is a number
 
const ageIsValid = (currentAgeIsInteger);

Number.isInteger()

Math.random()
Number.isInteger(randomUserID); 




